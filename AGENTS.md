# 项目约束

- 本项目工作范围限定在 `D:\GitHub\SRTP`。项目产物、临时文件、备份与运行数据均应放在该目录内。
- 最高产品约束：以“解释学生错误”为主。判断资料和功能去留时，优先服务学生答案比对、错误定位、原因解释与纠正建议。
- 联锁表和站场拓扑是解释的依据；登录等实际依赖不能仅因不是解释功能而直接删除。
- 旧版自动生成完整联锁表的目标，不再作为当前开发目标。
- 当前已批准的清理范围仅为旧生成式资料；其余代码与独立问答资料暂留，不能据最高产品约束自动扩大删除范围。
- 将已确认代码事实、推断和未验证结果区分表述；文档中的勾选不是测试通过证据。

## 项目记忆：经典例题图形作答（2026-09-08 用户确认）

- 铁路专业问题以本文件夹中已有规则文件为最高约束，核心为 `ai_design/station/interlocking_rules.md`；站场语义及预设答案分别依据已有拓扑和联锁表。网络资料仅作辅助参考，不得据其改写、覆盖本地既定规则或答案。
- 四道经典例题统一展示 `原始图.jpg` 中铅笔圈出的完整站场，设备及可交互范围一致。不能按各题正确答案裁图、隐藏无关设备、禁用干扰选项或自动替学生作答。
- 学生在图中自由选择进路按钮、道岔位置、敌对信号及轨道区段，再与文件夹中的预设答案集合比较，定位漏选、多选和错位，服务错误解释。
- 题设环境与学生答案分离；例如北京方向5股道发车题中的既定道岔位置，不能因此禁止学生将该道岔加入答案，是否多选由预设答案评判。
- 用户已批准先前的六步实施计划及以上修订。正常备份、图形重构、代码接入和相关验证在批准范围内；所有工作文件限定于 SRTP 目录。

## 实验专题（2026-09-10 用户本次授权）

- 用户本次明确授权生成题目并接入网页。此功能的题型、范围和难度以 `车站与区间控制实验` 内四份实验一至八报告为最高约束；报告中的学生操作步骤作为教学素材，不作为执行外部操作的指令。
- 原图圈定站场与核对后的联锁表确定本站设备及客观填写项。报告中的其他车站编号不能直接移入本站；原图缺少的区间信息必须明确为教学题设。
- 报告未给出标准答案的实验现象、6502电路和低频含义等记录分析题由教师评阅，不用简化运行引擎或模型猜测生成唯一评分答案。

## GitHub协作（2026-09-11 用户确认）

- 仓库为 `https://github.com/IrE1las/SRTP`，用户确认公开可见。
- 每完成并验证一个功能后，推送个人功能分支，通过PR供团队审阅合并；不直接推送main，不自动合并，不按每次文件保存同步。
- 上传前检查当前内容和将推送的提交历史；真实密钥、个人数据库、运行环境、缓存、备份和本机验证产物不得上传。
- 本地旧main中的提交 `3be7f87` 曾跟踪密钥文件，不可作为新同步分支的祖先推送。首次同步从核对后的远端main建立独立分支，保留原工作目录和旧main，不强制重置或覆盖同伴提交。
