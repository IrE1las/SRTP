# 经典例题 AI 解析约束与异常处理

更新：2026-09-09。适用目录：`D:\Codex\Projects\SRTP`，适用范围：四道经典例题。

本次对现有 DeepSeek 调用增加了专业事实约束、输出核验、异常恢复与页面状态管理。运行模型仍为 `deepseek-v4-flash`。这次修改是应用层约束，没有训练或修改模型权重。

## 专业依据与讲解方式

最高约束保持为 `ai_design/station/interlocking_rules.md`。站场语义读取 `station_topology.json`；预设答案及道岔的走行、防护、带动角色读取 `interlocking_table.csv` 中进路号 2、3、4、9。上述三个既有文件和预设判分集合均未修改。

新增 `classic_exam_profiles.json` 保存四题明确的换股道次序和题设条件。它是已有事实的结构化记录，不取代原规则、拓扑或标准答案。服务读取时核验换股道的连接、反位、实际使用角色、终到股道、条件区段，以及 CSV 与现有判分答案的一致性。

例如北京方向 5 股道发车，列车依次经 21、9/11、1/3 反位换股道；13/15 按 CSV 属于防护道岔，不能出现在实际换股道次序中。东郊至 4 股道的 23/25 属于带动道岔，解释会说明其与 17/19 同处 17-23DG 区段、锁闭前带动对平行作业的作用。北京发车题中 23/25、5/7 定位是题设条件，不会自动加入学生道岔答案；25DG、5DG 则是需要检查的条件区段。

每次请求的处理顺序：

1. 服务器重新比对本次答案，忽略客户端传入的旧错误列表。
2. 按每一处漏选、多选、错位生成讲解条目，包含“错在哪里、为什么、如何纠正、不纠正的影响”。专业句子由依据本地规则编写的模板及本题事实构成。
3. 模型仅组织这些条目的顺序，并在已有的简要／详细原因之间选择；首先陈述走行事实。模型必须返回限定 JSON 计划，不能提交任意专业正文或重新判分。
4. 程序核验每个错误编号恰好出现一次、没有缺项、重复、陌生编号、额外字段或不允许的取值，再由本地条目组成最终解释。

因此，模型即便写出“13/15 是实际走行道岔”这样的任意正文，也不会直接显示给学生。模型的思考文本不作为解析正文。学生仍可自由操作全图，由原预设答案判定对错。

解释模板绑定当前已核对的规则文件 SHA256。如果规则文件被更改，服务会要求重新核对解释模板和资料版本，返回明确的资料不一致提示，不会自动使用旧规则说明。不能为了消除提示而仅更新 `reviewed_rules_sha256`；必须先核对规则变更对应的模板、路线事实及测试。拓扑或 CSV 更新也应按相同步骤审查，自动一致性检查不能替代新增题目的专业审校。

## 异常与恢复

| 情况 | 服务行为 | 页面反馈 |
| --- | --- | --- |
| 没有错误 | 本地直接说明全部正确，不调用模型 | 正常判分页面不显示错误解析按钮 |
| 空字符串、空白、正文为 null、缺少 choices | 自动重试一次 | 仍失败时显示完整的“规则解析”，说明模型未返回有效内容 |
| 回复被截断或结束原因异常 | 不使用半截回复；允许一次重试 | 重试失败后显示规则解析及对应说明 |
| JSON 无法解析、额外正文、错误条目缺漏／重复／越界 | 拒绝该计划；允许一次纠正请求 | 不通过核验时明确显示规则解析 |
| 模型超时、连接失败、服务端 5xx | 有限重试 | 保留判分和答案，显示规则解析及稍后重试提示 |
| 429 限流 | 不自动重复请求 | 规则解析，并提示稍后再试 |
| 缺少密钥、400／401／402／403／404／422 配置或授权问题 | 不自动重试这些明确失败 | 规则解析，并提示联系教师检查配置 |
| 本进程已有 3 个不同解析请求执行中 | 不继续排队发出付费请求 | 立即显示规则解析，提示稍后重试 |
| 本地规则、答案或路线事实不可用／不一致 | 不编造依据；返回 HTTP 503 | 显示资料异常提示，已有判分仍可查看 |
| 浏览器收到空 200、损坏正文或条目与判分不符 | 拒绝显示不完整结果 | 显示错误提示，恢复按钮；答案和判分保留 |
| 再次尝试解析失败 | 保留重试前已显示的有效解析 | 同时显示本次失败提示 |
| 修改答案、重置、切换题目 | 取消旧浏览器请求，并用请求编号防止迟到结果覆盖 | 清除旧答案对应的判分和解释，新答案需重新提交 |

外部模型失败的规则解析使用 `source=rules_fallback`，不会伪装成 AI 成功。正常通过核验时使用 `source=ai`。用户可以据页面的“AI 辅助解析／规则解析”和状态提示区分结果来源。

## 调用与资源控制

- 以显式非思考模式组织已经确定的事实，设置 `thinking.type=disabled`、`temperature=0`、`response_format=json_object`，非流式返回。
- 关闭 OpenAI 兼容 SDK 的隐式重试。单次解析最多 2 次模型调用，且在启动调用前检查 58 秒预算。HTTP 读超时通常为 25 秒，连接 5 秒、写入 10 秒、连接池等待 5 秒。这些是分阶段超时与调用预算，不是严格的全流程墙钟上限；浏览器仍有 120 秒等待上限。
- 限制请求的设备数量、设备名称长度和道岔位置；本站场清单之外的设备不进入提示词。模型计划最多 80 个条目、正文最多 40,000 字符。
- 完整成功结果按用户、密钥版本、模型、地址、题目资料及错误事实缓存 120 秒，每进程最多 128 项；规则降级结果不缓存，便于恢复后重试。相同用户、相同输入的并发请求共享一次进行中的调用。
- 记录请求编号、请求与响应模型名、尝试次数、耗时、结束原因、token 数和核验结果。新增日志不记录密钥、模型正文、思考过程或学生身份。
- 教师设置页的“测试连接”也检查结束原因和非空正文。仅返回 HTTP 200、但正文为空或截断的情况，不再提示连接测试成功。该测试验证接口可用性，不替代题目专业内容验证。

DeepSeek 官方说明 JSON 模式仍可能产生空正文，所以使用 JSON 不能替代程序核验；参考 [JSON Output](https://api-docs.deepseek.com/guides/json_mode/) 与 [Thinking Mode](https://api-docs.deepseek.com/zh-cn/guides/thinking_mode/)。专业判断始终服从本项目本地规则。

## 验证结果

最终证据目录：`.planning/2026-09-08-constrained-ai/`。

真实模型调用在最终后端重启生效后进行，均为 HTTP 200、`source=ai`、`cached=false`。响应模型名经服务日志确认为 `deepseek-v4-flash`，结束原因为 `stop`；均为首轮请求通过核验。

| 案例 | 错误数 | 点击至收到响应耗时 | 最终解释字符数 |
| --- | ---: | ---: | ---: |
| 东郊至 III：按钮漏选、道岔错位、敌对漏选／多选、区段漏选 | 5 | 3.15 秒 | 869 |
| 东郊至 I：防护漏选、换股道错位、终端按钮类型错误 | 4 | 2.16 秒 | 756 |
| 东郊至 4：带动漏选、换股道错位、区段漏选 | 3 | 2.25 秒 | 645 |
| 北京 5 股道发车：防护、题设多选及条件区段漏选 | 4 | 2.70 秒 | 803 |
| 北京 5 股道发车：整题空答 | 18 | 3.25 秒 | 3,076 |

模型返回的是讲解计划；表中的字符数是组装后显示给学生的解释长度。耗时是本次观测，不构成后续速度保证。

- `live-report.json`、`provider-metadata.log`：上述最终实际调用、逐项覆盖与模型信息。五组截图包含结果与页面状态。
- `pytest-report.txt`：全量后端 **97 项测试通过**，覆盖现有功能、答案对照、角色说明、空答、干扰选项、空回复、截断、错误 JSON、越界内容、超时、限流、授权失败、缓存与并发、资料变更及连接测试。存在现有依赖／时间 API 弃用警告。
- `browser-fault-report.json`：**16 组检查通过**，包括规则降级及重试、空 200、null、损坏 JSON、缺项、重复、错误编号、伪全部正确、HTTP 503、断网、重复点击、三种取消请求情况，以及 1100px 下的安全文本显示。这里仅模拟 AI HTTP 响应和故障，判分接口是真实本地后端；这些记录不代表外部服务实际发生了相同故障。
- `build-report.txt`：最终前端构建通过，保留既有依赖注释和主包体积警告。
- `conditional-errors-result.png`：最终条件题解析预览；`rules-fallback-result.png`：模拟外部空回复后的规则解析预览。
- `backend-reload.json`：确认本项目后端进程归属后重启，使最终代码生效。前端地址仍为 `http://127.0.0.1:5173/`。
- `live-report-first-run.json`、`live-report-before-service-reload.json` 和 `before-reload-*.log` 为中间记录；最终结论以 `live-report.json` 与 `provider-metadata.log` 为准。

这些检查确认当前四题的已覆盖输入与故障处理，不代表任意新增车站、规则或题目均已专业验证。对于不属于当前题型、需要自由追问的解释，需另行设计和审校相应事实与交互。

## 实现与恢复

- `framework/backend/app/services/classic_exam_facts.py`：资料核验与专业解释条目。
- `framework/backend/app/services/classic_exam_ai.py`：有限计划、输出核验、重试、降级、缓存和并发。
- `framework/backend/app/api/interlocking_exam.py`：服务器重新判分、输入限制及结构化响应；原 `ai_explanation` 文本字段保留。
- `framework/backend/app/api/settings.py`：连接测试的正文和失败判断。
- `framework/frontend/src/components/ClassicExam/ClassicExam.vue`：解释卡片、来源、重试、完整性核验和旧请求取消。

修改前文件保存在 `.backups/2026-09-08-constrained-ai/manifest.json` 所列相对路径；最终修改与新增清单、前后 SHA256 见证据目录的 `change-manifest.json`。未修改数据库结构、现有模型密钥或已保存的模型配置。

如需恢复本次 AI 改动，应将清单中有备份的前后端文件作为一组恢复，重新构建前端并重启本项目后端，避免新页面与旧接口混用。新增服务和题型事实文件在旧代码恢复后不会被引用，可以先保留。无需恢复数据库或密钥配置；规则、拓扑、原图和标准答案不需要恢复。
