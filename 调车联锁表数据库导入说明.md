# 调车联锁表数据库导入记录

导入日期：2026-09-10。用户已明确确认核对完毕并授权导入。

## 导入结果

- 活动数据库：`framework/backend/railway.db`。
- 数据集：`original-station-shunting`。
- 站场标识：`original-photo-station`，关联 `pictures/原始图.jpg` 和 `ai_design/station/station_topology.json` 的五股道站场（5、III、I、II、4）。
- 新增 `shunting_datasets` 1 条数据集记录、`shunting_routes` 35 条调车进路，进路号码连续为 20—54。
- Excel 来源：`outputs/01a08987-4697-7603-b9c1-39b48954f274/调车联锁表_图片识别_人工核对稿_修订版.xlsx`。
- 导入前检测到 Excel 打开实例处于未保存状态，已通过 SaveCopyAs 保存该实例快照；快照文本与磁盘稿一致。实际入库依据为该快照。
- Excel 原核对状态仍为“待核对”，作为原始记录保留；数据集 `source.reviewed=true`，`source.review_confirmation` 记录用户本次明确确认，不依靠旧状态栏推断审批结果。

## 数据及角标保留方式

每条进路保留全部 15 个原表字段（信号机名称、显示分列）、空值、括号、换行、条件前缀和原顺序。合计 525 个字段值。

`fields` 保存可检索的原文；`rich_text` 对每个文本字段保存字符段，每段包含 `text` 与 `vertical_align`（baseline/subscript/superscript）。共保留 262 处下标字符段。后续网页或表格显示应使用字符段恢复角标，不能只将 `fields` 平写展示。

`provenance` 保存 Excel 行号、图片来源、图中位置、原识别说明、核对状态和人工修订栏。数据集记录核对稿及原图、拓扑、规则文件的 SHA-256 校验值，并保存导入时的拓扑快照。

大括号 `{23/25}` 已按用户确认保留。32 号 `<21>21DG IIIG`、44 号 `25G`、50 号 `<1>D1`、50/54 号 `1/19WG` 等内容按最终核对稿保存，没有在入库时自动改写或展开条件适用范围。

## 与项目现有数据的关系

原运行表 `interlocking_tables` 对应旧四股道演示站 `DOWN_THROAT_STANDARD`，字段不能完整表达本次原图资料。新增数据存入同一活动数据库中的专用资料表，通过原图站场标识和拓扑快照关联，未混入旧演示站运行进路。

本次没有修改既有演示站的 22 条运行进路、四道经典例题答案、用户、练习、作答记录和运行状态；原有 12 张业务表的行数与内容摘要均与导入前一致。

题目生成模块已完成准备，但在获得单独授权前不自动生成或发布题目。活动数据库当前仅保留已核对导入的 35 条原始进路；`shunting_questions` 表为空。获得授权后可按每条进路生成一题，题目答案使用四类字段：排列进路按钮、道岔位置及普通/防护/带动作用类别、敌对信号、轨道区段。方括号/大括号角色和尖括号条件保存在教师答案接口中，学生提交只返回错误类别和得分，不直接返回答案。

## 读取接口

以下接口使用项目现有 Bearer 登录认证，只允许教师与管理员读取答案资料。

| 接口 | 内容 |
|---|---|
| `GET /api/exam/shunting/dataset` | 数据集、核对来源、原图站场快照、进路数量 |
| `GET /api/exam/shunting/routes` | 按进路号码排序的 35 条目录 |
| `GET /api/exam/shunting/routes/{route_number}` | 单条进路的全部原文、角标字符段和来源信息 |
| `POST /api/exam/shunting/questions/generate` | 教师/管理员在获得授权后生成或刷新 35 条题目（幂等；当前未执行） |
| `GET /api/exam/shunting/questions` | 登录学生、教师或管理员读取题面与候选项，不含答案 |
| `GET /api/exam/shunting/questions/{question_id}` | 读取单题题面与候选项 |
| `POST /api/exam/shunting/questions/{question_id}/submit` | 学生提交四类答案并获得错误定位与得分 |
| `GET /api/exam/shunting/questions/{question_id}/answer` | 教师/管理员读取答案规格与原表来源 |

代码位于 `framework/backend/app/api/shunting_data.py`、`framework/backend/app/api/shunting_questions.py`、`framework/backend/app/services/shunting_data.py`、`framework/backend/app/services/shunting_questions.py` 和 `framework/backend/app/models/shunting_route.py`。后端启动时注册数据模型及接口。未启动常驻服务时，数据库导入和题目生成结果仍然持久保存。

## 可重复导入

保真载荷：`ai_design/station/shunting_interlocking_reviewed.json`。该载荷由核对稿快照提取，不能用首稿的 `transcription.json` 替代。

在 `framework/backend` 目录下，用项目 Python 运行 `scripts/import_shunting.py` 可先只读校验。正式导入需同时传 `--apply` 和位于项目内的新 `--backup-dir`。命令先生成 SQLite 一致性备份，再在一个事务中写入数据。

数据集编号与进路号码构成唯一约束。相同载荷重复导入不会新增重复记录或改变现有记录编号。缺行、重复编号、错误站场、缺少核对确认、原文与字符格式不一致等输入会拒绝导入。

## 验证与备份

- 29 项相关测试通过：包含保真回读、重复导入、事务回滚、损坏输入拒绝、教师权限、题目生成与答案隔离接口，以及原四道经典例题和旧运行引擎回归。
- 真实活动数据库以只读方式回读，525 个原文字段和全部角标记录逐项匹配。
- SQLite `integrity_check` 结果为 `ok`；新增调车数据无外键异常。
- 已有数据库存在 16 处旧记录引用用户的外键异常：`exercises` 1 处、`exercise_sessions` 15 处。导入前备份中已存在，导入后未增加，本次未修改这些旧数据。
- 原始图片、拓扑、规则及经典 CSV 校验值未变。

备份目录：`.backups/20260910-184907-shunting-import/` 保存导入前状态；误触发题目生成后已在 `.backups/20260910-193000-shunting-questions/railway-before-apply.db` 留存可恢复快照，随后撤回未授权的 35 条题目并在 `.backups/20260910-190959-revert-unapproved-shunting-questions/railway-before-revert.db` 留存撤回前快照。当前活动数据库题目表为空。恢复任一快照前应停止服务，并确认不会覆盖导入后新增的其他业务数据。

执行与验证记录位于 `.runtime/shunting-import-20260910/`。恢复数据库前应停止服务，并确认不会覆盖导入后新增的其他业务数据。
