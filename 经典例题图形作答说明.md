# 经典例题图形作答说明

更新：2026-09-09。项目目录：`D:\Codex\Projects\SRTP`。

## 入口与操作

启动项目后访问 <http://127.0.0.1:5173/student/lobby>，从“经典例题”点击“开始练习”。四道题均展示同一个铅笔圈内站场，可在页面上方切换题目。

1. 点击信号机，在站场上方操作条选择列车按钮或调车按钮，或标记为敌对信号。这些答案分别记录，互不替代。
2. 点击任一道岔端点，在操作条选择定位、反位或清除选择；双动道岔两端同步显示。
3. 点击轨道线或区段名称，选择／取消需要检查的区段。
4. 点击S5、SIII、SII、S4对应的出站信号机，在同一个操作面板中可分别标记列车敌对与调车敌对（S5D、SIIID、SIID、S4D）；图中不将调车功能画成另一架独立设备。北京方面的SLZA为发车终端按钮。
5. “我的答案”显示当前选择，点击答案标签可移除。“文字辅助操作”可展开，与图片实时同步。
6. 点击“提交检验”。错误详情包含漏选、多选和道岔错位；点击错误项回到图中定位。可按需点击“AI 解析错误原因”。

画面支持缩放、拖动空白区域平移、Ctrl+滚轮缩放、“全图”复位。信号、道岔、区段及终端按钮支持键盘聚焦和 Enter／空格操作。查看原图按钮以旋正方式展示原照片，原始文件未做加工覆盖。

信号机按最初照片中的具体设备图形绘制：保留灯位排列、同心圆、斜线、朝向及编号下标，不再按高柱／矮型分类附加特殊造型。D系列取消额外的整块梯形底座，改为照片中的短竖线与双圆；X与XD各自保留原有第四灯位纹样，S5/SIII/S4保留照片本身的上下两排灯位与窄矩形机构，SII保留横排四灯位。

最新间距修订中，单排信号中心距所属轨道24个SVG坐标单位，照片中上下两排的信号为30；符号边缘至轨道线边缘约10–11个单位（按几何轮廓测量，不计符号描边外扩）。同步收紧透明点击热区、避让右侧区段名称。最新预览与验证记录位于 `.planning/2026-09-08-signal-photo-spacing/`；原图核对片段仍在 `.planning/2026-09-08-signal-symbols/`，各旧目录截图保留为前版记录。

后续绝缘标记修订：钢轨绝缘统一使用单条短竖线，与对应信号机的机柱对齐；S5/SIII/S4按原照片的窄矩形机构中线对齐。D7、D9共用同一处分界标记。此修订后的最新全图见 `.planning/2026-09-08-insulation-alignment/station-preview.png`。

## 四题同图与规则边界

- 专业规则以项目已有 `ai_design/station/interlocking_rules.md` 为最高约束。设备连接和区段范围来自 `station_topology.json`；四题预设答案对应 `interlocking_table.csv` 中进路号 **2、3、4、9**。CSV以GB18030编码读取。
- 本次未修改规则文件、拓扑文件或后端预设答案集合。后端仍按进路按钮、道岔位置、敌对信号和轨道区段四类集合判定，不额外对按钮先后顺序或防护／带动标记单独计分。
- 四题共享15个清单信号、4个出站兼调车功能、8组道岔、18个区段以及SLZA终端。原图另有预告信号，按描述性名称“原图预告信号”保留点击及敌对选择，未编造正式编号或列车／调车按钮。它不属于当前四题预设敌对集合，选入后会被判为多选。
- 北京发车题中23/25、5/7定位是题设环境，独立于学生答案；图中仍可将它们加入答案，由后端判多选。重置答案不会把环境条件自动填入学生答案。
- 画面颜色表达作答选择，不等同于实际信号灯显示、区段占用或真实联锁锁闭状态。
- 修改答案后立即清除旧判分与旧解释，需重新提交。AI请求使用已提交快照；修改、重置或切题会取消旧解析请求。解释按错误卡片呈现，模型只编排经本地规则约束的讲解内容，页面区分“AI辅助解析”和模型不可用时的“规则解析”。

## AI解析的最新改进

2026-09-09已完成模型输出约束和异常处理：每项错误均包含错在哪里、为什么、如何纠正及不纠正的影响；防护、带动、实际走行和题设条件分别说明。空回复、截断、格式错误、连接失败及服务异常采用有限重试和明确标识的规则解析；重试失败不会清掉已有有效解析或本次判分。详细边界与维护方法见 [AI解析约束与异常处理说明](./AI解析约束与异常处理说明.md)。

最终四题错误案例及一组整题空答均通过真实 `deepseek-v4-flash` 调用，耗时2.16–3.25秒，均未命中缓存；空答的18处错误完整返回3,076字解释。全量后端97项测试、16组浏览器异常与交互检查及前端构建通过。最终记录在 `.planning/2026-09-08-constrained-ai/`，其 `live-report.json`、`provider-metadata.log`、`pytest-report.txt` 与 `browser-fault-report.json` 分别记录真实调用和测试；异常注入不代表外部模型真实发生故障。前版视觉验证记录继续保留。

## 实现位置

- `framework/frontend/src/domain/classicStation.js`：拓扑到绘图坐标的映射、四题元信息、答案初始化。
- `framework/frontend/src/components/ClassicExam/ClassicStationCanvas.vue`：统一SVG站场、设备交互、缩放平移、错误定位。
- `framework/frontend/src/components/ClassicExam/RailwaySignalSymbol.vue`：按原图描摹的信号机专业符号及灯位纹样。
- `framework/frontend/src/components/ClassicExam/ClassicExam.vue`：操作条、统一答案、提交与AI解释、图文同步。
- 原四个 `InterlockingExam*.vue` 页面保留为共用组件的题目入口。
- `framework/backend/tests/test_classic_exam_contract.py`：以既有CSV为独立依据的答案和错误判定回归。

## 验证证据

最新绝缘标记修订记录在 `.planning/2026-09-08-insulation-alignment/`：`geometry-report.json`验证四题同一套18条单竖线、15个清单信号与其绝缘横坐标对齐，以及1600px／1100px窗口下的信号和轨道点击；`build-report.txt`记录本轮构建通过，`progress.md`记录修改依据与范围。本轮没有重跑后端判分或调用外部AI。

最新信号样式／间距修订记录在 `.planning/2026-09-08-signal-photo-spacing/`：

- `station-preview.png`、`symbols-preview.png`：最终全图和符号放大预览。
- `geometry-report.json`：1600px和1100px窗口下，16处信号均可点击；18个区段均通过直接点击轨道线选择和取消。测量未发现信号名与区段标签重叠，信号热区未拦截下方轨道中心线点击。
- `browser-report.json`：四题分别读取原CSV预设答案，通过图片输入后均通过真实后端判分；题设道岔多选、错误定位、图文同步、键盘、缩放平移及切题重置也通过。AI响应在这轮浏览器回归中使用替身，只验证请求快照和安全显示，不代表本轮再次调用了外部模型。
- `build-report.txt`：前端构建通过；仍有既有依赖注释及主包体积警告。
- `change-manifest.json`：本轮修改清单及修改前后SHA256。

最初完整接入的验证记录在 `.planning/2026-09-08-classic-diagram/`：

- `pytest-report.txt`：25项后端测试通过，包括新增9项四题答案／错误判定检查。
- `build-report.txt`：最终前端构建结果；仍有原有依赖注释及主包体积警告。
- `browser-report.json`：实际浏览器四题同设备清单、纯图形正确作答、题设道岔多选、错误定位、图文同步、键盘、缩放拖动、1100px窗口及切题重置检查。
- `live-ai-report.json`：一次真实外部模型调用成功，HTTP 200，返回674字符；其请求与已提交答案完全一致。该记录证明本例接口及显示链路可用，不代表所有专业解释均完成教师审校。
- `01-full-station.png`、`01-correct.png`至`04-correct.png`、`error-localization.png`、`narrow-1100.png`、`original-reference.png`、`live-ai-explanation.png`：页面和结果截图。
- `browser-report-before-final.json`、`failure.png`保留中途验证痕迹；最终是否通过以 `browser-report.json` 为准。

## 备份与恢复

本次AI改动前的文件在 `.backups/2026-09-08-constrained-ai/`，恢复时按清单成组恢复前后端并重启后端、重新构建前端。详细清单及规则文件变更后的审校要求见AI解析说明。

最新绝缘标记修订前的 `ClassicStationCanvas.vue`、`RailwaySignalSymbol.vue` 和本说明在 `.backups/2026-09-08-insulation-alignment/`，文件清单与SHA256见该目录 `manifest.json`。

本轮信号样式／间距修订前的三个前端文件和本说明备份在 `.backups/2026-09-08-signal-photo-spacing/`，`manifest.json`记录原路径与SHA256。若只恢复本轮显示调整，可按相对路径恢复其中三个前端源文件后重新构建。

修改前文件在 `.backups/2026-09-08-classic-diagram/`，`manifest.json`记录原路径和SHA256。原四个例题页面、练习大厅页面及AGENTS.md均有备份。

如需恢复旧界面，可将备份中的四个 `InterlockingExam*.vue` 与 `ExerciseLobby.vue` 按原相对路径覆盖回前端源目录并重新构建。新增共用组件届时不再被入口引用，可保留。无需恢复数据库；本次图形改造没有数据库结构变更，也没有更换预设专业答案。

用户确认的规则最高约束及四题同图同交互已写入根目录 `AGENTS.md` 的项目记忆段落。
