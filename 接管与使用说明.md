# 管理员接管与使用说明

更新：2026-09-08。项目唯一工作目录：`D:\Codex\Projects\SRTP`。

## 登录与启动

- 前端：<http://127.0.0.1:5173/>
- 用户名：`admin`
- 密码：`00000000`（按用户明确指定设置，数据库保存 bcrypt 哈希）
- 管理员入口：<http://127.0.0.1:5173/admin>
- API 文档：<http://127.0.0.1:8000/docs>
- 以后双击根目录 `启动项目.bat`，会启动前后端、检查响应并打开 Edge。
- 关闭浏览器不会停止后台服务；需要停止时执行根目录 `停止项目.ps1`。

应用当前仅监听本机回环地址。浏览器使用项目内 `.runtime/browser-profile`，登录状态与浏览器资料保存在项目内。

## 主要使用流程

### 学生错误解释（最高优先级）

左侧提供基础进路、防护道岔、带动道岔、条件区段四个专题。进入后选择按钮、道岔位置、敌对信号、轨道区段，再点击“提交检验”。程序先按标准答案定位漏选、多选、错位；有错误时可点击“AI 解析”，获取原因和后果说明。

AI 设置页可保存密钥、模型与地址，保存后会用于专项解析。当前模型配置为 `deepseek-v4-flash`，已实际调用成功。密钥未写入本说明。

### 现有通用功能

- 系统管理：查看用户和全局数量；保留现有用户，禁止删除当前管理员或取消其管理员身份。
- 题库管理：管理员可查看全部教师题目并管理题目。
- 新建练习：填写目标始端、终端、限时和发布状态。
- 练习大厅：开始练习，在站场点击始端信号，再点击终端按钮；例如先点 X 信号，再点站场上方 I 按钮。
- 历史与回放：练习完成后查看本地规则评分、状态快照和操作顺序。
- 联锁表学习：查看通用站场与参考进路。
- 学习概览、教学统计：个人练习统计及管理员全局教学统计。

管理员直接拥有上述学生/教师页面访问能力，无需切换账户角色。公开注册不再允许创建管理员。接管时已轮换 JWT 签名密钥，旧登录令牌失效；其他账户和已有业务记录保留。

## 本次修复

1. 在项目内建立新的 Python 3.12 环境，替代启动脚本中指向他人电脑的旧虚拟环境。
2. 重设 admin 账户、统一活动数据库定位，避免从不同启动目录误用另一份 railway.db。
3. 补齐管理员跨角色权限、四个专项题菜单及全部教师题目的管理列表。
4. AI 配置保存使用标准 dotenv 解析，避免引号进入密钥引发认证失败。
5. 专项解析使用管理员设置的模型/地址，接入解释规则，并精确筛选对应 CSV 题目。
6. AI 错误列表由后端重新计算，避免信任客户端传入的错误结论。
7. 添加通用站场终端按钮，修复浏览器对 UTC 倒计时的解释。
8. 普通失败操作保留具体错误消息，便于回放定位。

## 已验证结果及范围

- 后端：16 项自动测试通过，包括原有12项及接管权限、四题答案/错误、后端错误重算、带引号配置解析测试。
- 前端：构建成功；13个页面实际浏览器导航通过。
- 浏览器：admin 登录、四专题错误提交、创建题目、X→I 办理完成、历史/评分/回放、AI 设置保存已验证。
- 外部 AI：设置页连接测试成功；基础专题真实错误解析成功，返回1550字符中文解释。其余专题答案比对已测，不代表每个专题每种错误均做了外部模型验收。
- 保留一条“管理员接管验证：X 至 I 道”演示题及其完成记录，便于直接查看回放。
- Python 测试存在弃用警告，前端存在依赖注释和大体积包警告；未阻止上述检查通过。

本次是本机单管理员功能验证。未声称通过多学生并发隔离测试；现有通用站场仍按 station_id 共享状态。普通评分仍为本地规则评分，未改成大模型评分。独立知识问答实验暂留，未作为本次 Web 验收项运行。模型解释的专业准确性仍需要教师审核。

## 文件与恢复

- 活动数据库：`framework/backend/railway.db`。
- 原有 `framework/railway.db` 暂留；启动器不使用它。
- 新运行环境：`.runtime/venv`；旧 `framework/backend/.venv` 未删除。
- 后台日志：`.runtime/backend.log`、`.runtime/backend.error.log`、`.runtime/frontend.log`、`.runtime/frontend.error.log`。
- 已验证的后端依赖快照：`framework/backend/requirements.lock.txt`。
- 接管前备份：`.backups/2026-09-08-admin-takeover/`，包括两份 SQLite 一致性备份、原后端环境配置和修改前源码压缩包。
- 验证记录与截图：`.planning/2026-09-08-takeover/`。首次浏览器报告保留排错记录，AI修复后结果见 `ai-report.json`。

恢复数据库或配置前先停止服务，再按备份原位置恢复，避免覆盖仍在使用的数据库。
