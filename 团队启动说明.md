# 团队启动说明：启动器 v2

## 组员只需做什么

1. 同步包含新版启动器的完整分支。PR 合并前，需要在 GitHub Desktop 切换到 `codex/portable-launch-v2`；只同步旧 main 不会得到新版。
2. 将仓库放在 Windows 10/11 x64 的可写目录。中文、空格均可，不要直接在 ZIP 预览内运行。
3. 首次联网，双击根目录 `启动项目.bat`，等待窗口显示 `SRTP ready` 并打开浏览器。首次需要下载运行环境和依赖，可能持续数分钟。
4. 本机全新数据库的管理员为 `admin`，密码为 `00000000`。已有数据库中的账号保持不变。
5. 使用完成后双击 `停止项目.bat`。关闭网页或启动窗口不会停止后台服务。

`framework` 中的旧前端／后端 BAT 也统一转到根目录启动器，避免从其他入口再次误用系统环境。

## 为什么旧版在不同电脑、不同目录表现不同

- GitHub 副本与 Codex 副本代码版本不同，前者后来增加了部分首次环境准备。
- 旧 Codex BAT 使用 LF 换行并直接引用中文脚本名，在本机实际出现命令被截断的解析错误；新版使用纯 ASCII 命令和 CRLF，Git 属性保证再次克隆也得到 CRLF。不能把所有 LF 格式 BAT 都认定为不能运行。
- 旧脚本依赖全局 Node 和预先存在的 Python venv；GitHub 版仍需组员自己安装这些运行时。
- `.runtime` 和依赖目录本来就不应进入 Git。venv 记录创建机器的解释器路径，复制整个 venv 不能作为可移植环境方案。
- 旧版固定占用 8000/5173。两个副本同时启动时，后来启动的副本会失败；新版分配空闲端口并同步前端代理目标。
- 旧版前端只判断 Vite 文件是否存在，锁文件变化不会更新依赖；存在的 venv 也未检查能否执行。GitHub 版遗漏 `Start-Process -PassThru`，进程记录可能为空。

## 新版如何准备环境

`runtime-manifest.json` 固定 Python 3.12.14、Node.js 22.23.2、uv 0.12.13。启动器校验 uv 和 Node 下载文件的 SHA256；固定 uv 内置的下载元数据负责 Python 归档校验。Python 只从项目内安装的位置选取，不会退回到全局 Python。下载的程序均留在 `.runtime/bootstrap-v2`，无需修改系统设置或安装编译器。

后端使用 `requirements.lock.txt` 同步，并检查所有锁定包版本和关键原生模块导入；前端使用 `package-lock.json` 执行 `npm ci`，结合锁文件、Node 版本与项目路径判断是否需要更新。下载失败不会写成功标记，下次运行可重试。环境损坏时在 `.runtime` 内保留旧环境副本再重建；目录移动后按新路径建立 venv。

已有 `.env` 和 `railway.db` 不会被重置。首次创建的 JWT 密钥来自系统加密随机数；API 密钥仍由各自配置。教学资料导入保持可重复执行，不覆盖已有题库。

实现依据：[uv 管理 Python](https://docs.astral.sh/uv/guides/install-python/)、[uv 环境变量](https://docs.astral.sh/uv/reference/environment/)、[Node 官方下载及校验清单](https://nodejs.org/dist/v22.23.2/SHASUMS256.txt)。

## 常见情况

| 情况 | 处理方法 |
| --- | --- |
| 首次下载失败 | 保留错误窗口，检查其显示的下载域名是否可访问，再次双击。日志在 `.runtime/launch-v2.log`。不能仅靠 Git 同步解决被防火墙或代理阻断的下载。 |
| 同时开了两个项目目录 | 以各自窗口中的地址为准。新版不会占用、复用或停止另一副本的进程。 |
| 仍有旧启动器的服务 | 继续通过启动它们的旧目录关闭；新版停止脚本只处理带有新版所有权记录的服务。 |
| 服务启动后很快失败 | 检查 `.runtime/backend-v2.error.log`、`frontend-v2.error.log`；新版会回收本次启动的服务，避免半启动残留。 |
| 同步后页面仍是旧版 | 关闭旧页面并重新双击当前目录的 BAT。后端源码改动后，应先运行停止 BAT 再启动；不依赖开发服务器自动加载后端代码。 |
| 同时双击多次 | 同一目录有启动锁，等待第一次完成。完成后重复启动会打开现有健康服务。 |
| 没有 AI API 密钥 | 可以启动网站、登录和使用本地教学功能；真实模型解释需要单独配置密钥和联网。 |
| 想换路径或换电脑 | 同步完整源代码，在新位置双击，重新创建该位置的专用环境；不要复制 venv 代替初始化。 |
| 首次就完全离线 | 当前源码发行方式不支持。需另做包含全部运行时、依赖及许可证的离线发行包。 |

已准备完成且锁文件未变化时，可离线复用环境；需要强制禁止依赖下载时，在 PowerShell 使用：

```powershell
$env:SRTP_OFFLINE = '1'
powershell -NoProfile -ExecutionPolicy Bypass -File .\launch.ps1 -NoBrowser
```

这只限制运行环境下载，不会把真实 AI 接口变成离线功能。缺少必要缓存时会明确报错。

网络环境确有需要时，可在启动前设置 `SRTP_PYPI_INDEX` 或 `SRTP_NPM_REGISTRY` 为团队自行信任的 HTTPS 索引地址。不要关闭 TLS 校验。Node/uv/Python 运行时仍需访问其固定下载来源，npm 锁文件中的归档地址也可能仍指向官方仓库。

## 开发与验证

仅准备环境，不启动服务或浏览器：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\launch.ps1 -PrepareOnly -NoBrowser
$runtime = Get-Content .\.runtime\environment-v2.json -Raw | ConvertFrom-Json
& $runtime.python --version
& $runtime.node --version
```

前端构建必须让 npm 子进程也使用项目内 Node：

```powershell
$env:PATH = $runtime.nodeDir + ';' + $env:PATH
& $runtime.node (Join-Path $runtime.nodeDir 'node_modules\npm\bin\npm-cli.js') --prefix .\framework\frontend run build
```

后端测试应使用独立测试数据库，避免修改个人运行数据：

```powershell
$env:DATABASE_URL = 'sqlite:///' + (Join-Path $PWD '.runtime\test-launch-v2.db').Replace('\', '/')
$env:SECRET_KEY = 'local-test-only-not-a-production-secret'
Push-Location .\framework\backend
& $runtime.python -m pytest tests -q
Pop-Location
```

可通过 `-BackendPort 8100 -FrontendPort 5273` 指定优先端口，已占用时仍向后找空闲端口。需要自动化验证 BAT 时设置 `SRTP_NO_PAUSE=1`，BAT 会传递真实退出码；平常双击仍保留窗口提示。

## 同步边界

提交启动脚本、运行版本清单、依赖锁文件、源码和必要教学资料。`.runtime`、`node_modules`、`.env`、个人数据库、日志与备份均不提交。组员同步代码不会同步其他人的学生记录和教师录入数据。

本次开发阶段的实际验证明细保存在开发者本机 `.planning/2026-09-14-portable-launch`；该目录按项目约定不会进入仓库。支持范围是 Windows x64；未对每位组员的实际机器进行远程验证。

维护者可用开发 Python 执行 `scripts/verify_launch.py`，参数为本项目 `.planning` 下尚不存在的测试目录。它只复制可同步源码，清除子进程 PATH 中的 Python/Node，执行联网首次启动、离线重启、锁更新、损坏环境修复、登录及失败清理测试；保留日志，不操作原副本的服务。
